configuration <- list(
    suite_size = 1,
    rounds_per_simulation = 8,
    omega_size = 4,
    use_seed = TRUE,
    custom_seed = 4219 * 0106 * 413
)

contestants <- list(
    "cheater",
    "wrong",
    "player"
)
